#include <iostream>

#include "src/getPath.cpp"

using namespace std;

int main(int argc, char *argv[])
{
    // version info
    string version = "0.0.1";
    string art = "Splash coming soon";

    // splash
    // cout << version << endl;
    // cout << art << endl;

    // pull target dir after flag perhaps, -d?
    getPath(/* fill with call arg for dir */);


    return 0;
}